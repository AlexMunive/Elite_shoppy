import React from "react";
import MainProducts from "../components/MainProducts";

const Products = () => {
    return (
        <div>
            <MainProducts />
        </div>
    );
};

export default Products;
